# Render deployment config — deploy ClientOS for free
# 1. Go to render.com → New → Blueprint
# 2. Select this repository
# 3. Render will detect this render.yaml file
# 4. Set your environment variables in the Render dashboard

services:
  # ── NestJS API ──
  - type: web
    name: clientos-api
    runtime: node
    plan: free
    buildCommand: npm ci && npx prisma generate --schema=packages/database/prisma/schema.prisma && npm run build --workspace=apps/api
    startCommand: npx prisma db push --schema=packages/database/prisma/schema.prisma --accept-data-loss && node apps/api/dist/main.js
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: ENCRYPTION_KEY
        sync: false
      - key: CORS_ORIGIN
        sync: false
      - key: API_PORT
        value: "3001"
      - key: NODE_ENV
        value: "production"

  # ── Next.js Frontend ──
  - type: web
    name: clientos-web
    runtime: static
    plan: free
    buildCommand: npm ci && NEXT_PUBLIC_API_URL=$NEXT_PUBLIC_API_URL NEXT_PUBLIC_SCRAPER_URL=$NEXT_PUBLIC_SCRAPER_URL npm run build --workspace=apps/web
    staticPublishPath: apps/web/.next
    pullRequestPreviewsEnabled: true
    envVars:
      - key: NEXT_PUBLIC_API_URL
        sync: false
      - key: NEXT_PUBLIC_SCRAPER_URL
        sync: false

  # ── Python Scraper Service ──
  - type: web
    name: clientos-scraper
    runtime: python
    plan: free
    rootDir: apps/scraper
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: CORS_ORIGINS
        sync: false
      - key: PYTHON_VERSION
        value: "3.12.0"

databases:
  # ── PostgreSQL ──
  - name: clientos-db
    plan: free
    ipAllowList: []
